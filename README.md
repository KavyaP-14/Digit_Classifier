Description: Build a simple neural network to classify digits
using NumPy only (no ML libraries).
Challenges:
Implement forward propagation & activation functions
Calculate loss and accuracy
Visualize results
Tech Stack: Python, NumPy, Matplotlib
